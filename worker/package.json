{
  "name": "primis-forms-worker",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 migrations apply primis-forms --local",
    "db:remote": "wrangler d1 migrations apply primis-forms --remote",
    "check": "wrangler deploy src/index.js --dry-run --name primis-forms --compatibility-date 2026-08-18",
    "test": "node --test"
  },
  "devDependencies": {
    "wrangler": "4.123.0"
  }
}
