# Primis

The Primis website and Atlas product showcase.

## Development

```sh
npm ci
npm run dev
```

## Production build

```sh
npm run build
```

For production, configure the host to run `npm ci && npm run build` and publish the generated `dist/` directory.
